class BuchiSemantics(iSTR):
    initial(self):
    set self.initial
    actions(self, i, c):
    actions= self.delta[c]
        for a in actions:
            if a[0](i):
                active.apprend(a)
        return actives

__init(buchi)__
    ini, d, pred
    self.initial = ini
    self.delta=dbmself.pred=pred



execute(self,i,c,a)

return[a]


